{% extends 'base.html.twig' %}

{% block title %}MyBookApp - User edit{% endblock %}

{% block body %}

    <div class="container">
    <h1 class="mt-4">User infos edit</h1>

    {% for message in app.flashes('warning') %}
        <div class="alert alert-warning mt-4">
            {{ message }}
        </div>
    {% endfor %}

    {{ form(form) }}
    </div>

{% endblock %}
